version: 0.2

phases:
  install:
    runtime-versions:
      java:
  pre_build:
    commands:
      - aws s3 cp s3://team-c-p1-orm/target/orm-0.1.jar ~/.m2/repository/com/revature/orm/0.1/orm-0.1.jar
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/team-c-p1-webapp.jar